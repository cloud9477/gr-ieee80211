id: ieee80211_demod
label: demod
category: '[ieee80211]'

templates:
  imports: import ieee80211
  make: ieee80211.demod()

inputs:
- label: sync
  domain: stream
  dtype: byte
#  vlen: ...
#  optional: ...
- label: inSig
  domain: stream
  dtype: complex

outputs:
- label: outLlr
  domain: stream
  dtype: float
#  vlen: ...
#  optional: ...

#  'file_format' specifies the version of the GRC yml format used in the file
#  and should usually not be changed.
file_format: 1
