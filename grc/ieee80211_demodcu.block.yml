id: ieee80211_demodcu
label: demodcu
category: '[ieee80211]'

templates:
  imports: from gnuradio import ieee80211
  make: ieee80211.demodcu()

inputs:
- label: sig
  domain: stream
  dtype: complex

outputs:
- domain: message
  id: out

#  'file_format' specifies the version of the GRC yml format used in the file
#  and should usually not be changed.
file_format: 1
