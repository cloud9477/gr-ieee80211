id: ieee80211_encode
label: Encode
category: '[IEEE 802.11ac WiFi 5]'

templates:
  imports: from gnuradio import ieee80211
  make: ieee80211.encode()

inputs:
- label: inBits
  domain: stream
  dtype: byte

outputs:
- label: outChips
  domain: stream
  dtype: byte


file_format: 1
