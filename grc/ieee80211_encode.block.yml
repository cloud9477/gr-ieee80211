id: ieee80211_encode
label: encode
category: '[ieee80211]'

templates:
  imports: from gnuradio import ieee80211
  make: ieee80211.encode()

inputs:
- label: inBits
  domain: stream
  dtype: byte

outputs:
- label: outChips
  domain: stream
  dtype: byte


file_format: 1
