id: ieee80211_modulation
label: modulation
category: '[ieee80211]'

templates:
  imports: from gnuradio import ieee80211
  make: ieee80211.modulation()

inputs:
- label: inBits1
  domain: stream
  dtype: byte
- label: inBits2
  domain: stream
  dtype: byte

outputs:
- label: outSig1
  domain: stream
  dtype: complex
- label: outSig2
  domain: stream
  dtype: complex

#  'file_format' specifies the version of the GRC yml format used in the file
#  and should usually not be changed.
file_format: 1
