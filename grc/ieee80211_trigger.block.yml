id: ieee80211_trigger
label: trigger
category: '[ieee80211]'

templates:
  imports: import ieee80211
  make: ieee80211.trigger()

inputs:
- label: inAC
  domain: stream
  dtype: float
  # vlen: 1
  # optional: ...

outputs:
- label: trigger
  domain: stream
  dtype: byte
  # vlen: ...
  # optional: ...


#  'file_format' specifies the version of the GRC yml format used in the file
#  and should usually not be changed.
file_format: 1
